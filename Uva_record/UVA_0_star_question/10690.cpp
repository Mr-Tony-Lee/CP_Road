#include<bits/stdc++.h>
using namespace std;

int main(){
    int n , m ;
    while(cin >> n >> m ){
        vector<int> arr(n+m);
        int num_cnt = n+m;
        for(int i = 0 ; i < num_cnt ; i++ ){
            cin >> arr[i];
        }
        
    }
}